import React from "react";
import CreateCards from "./CreateCards";


const Configs = () => {
  return (
    // tengo que crear en esta card todo lo que quiero para mis cards y luego con props lo paso a lo que quiero que se muestre(preguntar a mati) 
    <section className="mt-32 mb-10">
      <CreateCards/>   
    </section>
  );
};

export default Configs;
